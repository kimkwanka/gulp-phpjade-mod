doctype html
html
    head
        title
            :php echo htmlspecialchars($title, ENT_QUOTES, 'UTF-8');

    body
        ul
            :php foreach ($this->list as $list):
            li.foo(class="bar")
                :php echo $list;
            :php endforeach
